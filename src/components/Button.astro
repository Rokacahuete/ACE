---
const { redirect, target, id } = Astro.props;
---

<a href={redirect} class="button" target=`${target ? "_blank" : ""}` role="button" id={id ?? ""}>
    <slot></slot>
</a>

<style>
    .button {
		font-family: "Super Dream", sans-serif;
        display: inline-flex;
        width: 120px;
        height: 60px;
        background-color: var(--button-color);
        border: 2px solid var(--border-color);
        border-radius: 10px;

        align-items: center;
        justify-content: center;

        text-decoration: none;

        transition: background-color .3s, transform .3s;
    }
    .button:hover {
        background-color: var(--button-hover);
        transform: scale(1.1);
    }
</style>